// Composition root: the single place where concrete adapters are wired to ports
// and the configured use-case graph is provided to the React tree via Context.
// This is the only module that imports from infrastructure/.
// Populated during infrastructure implementation tasks.
export {}
